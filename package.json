{
  "name": "ezroom",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "lint": "echo \"lint not configured\"",
    "build": "echo \"build not configured\""
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "@cucumber/cucumber": "^11.0.0"
  }
}

